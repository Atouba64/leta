import { colors, roleAccents } from './colors';
import { typography } from './typography';

export const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
};

export const radii = {
  sm: 10,
  md: 14,
  lg: 20,
  pill: 999,
};

export const theme = {
  colors,
  roleAccents,
  typography,
  spacing,
  radii,
};

export default theme;
