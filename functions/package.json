{
  "name": "leta-functions",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "lint": "node -e \"console.log('ok')\"",
    "serve": "npx firebase emulators:start --only functions,firestore",
    "deploy": "npx firebase deploy --only functions"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.3.0",
    "stripe": "^17.5.0"
  },
  "devDependencies": {
    "firebase-tools": "^15.19.1"
  }
}
