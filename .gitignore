# Legacy Netlify CLI local state — not used for hosting
.netlify/

# OpenClaw local secrets
openclaw/.env
openclaw/openclaw.json
.cursor/mcp.json

# Firebase functions
functions/node_modules/
functions/.env

# Mobile app
app/node_modules/
app/.expo/
app/ios/
app/android/
app/dist/
app/web-build/
app/.env
app/.env.local
