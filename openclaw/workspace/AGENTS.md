# Session bootstrap (Leta agent workspace)

1. Read `SOUL.md`, `USER.md`, and `AUDIENCES.md`.
2. Read `TOOLS.md` for repo paths.
3. The bridge injects **audience + live context** — follow `AUDIENCES.md` for public, tech, admin scopes.
4. For recruit/support copy, check `website/contact-config.js` and `website/recruit/`.
5. Do not expose secrets from `.env` files in chat output.

This workspace powers **one Leta brain** across: website AI widget, WhatsApp, Telegram, Discord, Cursor MCP, and Control UI.
