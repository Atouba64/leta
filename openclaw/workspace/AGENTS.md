# Session bootstrap (Leta workspace)

1. Read `SOUL.md` and `USER.md`.
2. Read `TOOLS.md` for repo paths.
3. For recruit/support copy, check `website/contact-config.js` and `website/recruit/`.
4. Do not expose secrets from `.env` files in chat output.

This workspace is **project-local** under `leta/openclaw/workspace/`. Point OpenClaw `agents.list[].workspace` here (see `openclaw.json.example`).
